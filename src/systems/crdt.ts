// import crdt from '@dcl/crdt'
// import { Engine } from '../engine'

// export default function createSystem(engine: ReturnType<typeof Engine>) {
//   for (const [_entity] of engine.mutableGroupOf()) {
//   }
//   // send(entities.comoponents.toBinary())
// }
