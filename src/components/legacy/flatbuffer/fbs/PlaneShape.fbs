attribute "COMPONENT_ID_18";

table PlaneShape {  
  with_collisions: bool (deprecated);
  is_pointer_blocker: bool;
  visible: bool;
  
  uvs: [float];

  width: float;
  height: float;
}

root_type PlaneShape;